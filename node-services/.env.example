# Node service environment example
# Do NOT commit secrets into VCS. Use secret manager in production.

# JWT secret must match Flask backend to share token validation if tokens are used across services
JWT_SECRET=your_jwt_secret_here

# DB connection for Node service (read-only or service role as appropriate)
DATABASE_URL=postgresql://user:password@host:5432/dbname

# When running trusted migration or worker jobs, set:
# APP_IS_SERVICE=true

# Logging
LOG_LEVEL=info
